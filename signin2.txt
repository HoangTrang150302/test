import 'package:flutter/material.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Hello World Demo Application',
      theme: ThemeData(
        backgroundColor: const Color(0xffffbb0b),
      ),
      home: MyHomePage(title: 'Home page'),
    );
  }
}

class MyHomePage extends StatelessWidget {
  MyHomePage({Key key, this.title}) : super(key: key);
  final String title;
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xFFFFBB0B),
      body: new Center(
        child: Column(
          children: <Widget>[
            Container(
              margin: EdgeInsets.fromLTRB(20, 10, 10, 0),
              padding: new EdgeInsets.only(top: 92.0),
              child: Text(
                "Đăng nhập",
                style: TextStyle(
                  fontSize: 44,
                  fontWeight: FontWeight.bold,
                  color: Colors.white,
                ),
              ),
            ),
            Container(
              margin: EdgeInsets.only(),
              padding: EdgeInsets.all(25),
              child: TextFormField(
                decoration: new InputDecoration(
                  filled: true,
                  fillColor: Colors.white,
                  labelText: "Username",
                  border: OutlineInputBorder(
                    borderRadius: BorderRadius.circular(10),
                  ),
                ),
              ),
            ),
            Container(
              padding: EdgeInsets.all(25),
              child: TextFormField(
                obscureText: true,
                decoration: new InputDecoration(
                  filled: true,
                  fillColor: Colors.white,
                  labelText: "Password",
                  border: OutlineInputBorder(
                    borderRadius: BorderRadius.circular(10),
                  ),
                ),
              ),
            ),
            Container(
              padding: EdgeInsets.all(25),
              child: TextFormField(
                decoration: new InputDecoration(
                    filled: true,
                    fillColor: Colors.white,
                    labelText: "Bạn muốn",
                    border: OutlineInputBorder(
                      borderRadius: BorderRadius.circular(10),
                    )),
              ),
            ),
            SizedBox(height: 20.0),
            MaterialButton(
              height: 38,
              minWidth: 250,
              shape: RoundedRectangleBorder(borderRadius: new BorderRadius.circular(12)),
              onPressed: () {
                Navigator.push(
                  context,
                  MaterialPageRoute(builder: (context) => SecondRoute()),
                );
              },
              child: Text(
                "Đăng ký",
                style: TextStyle(
                  fontSize: 24,
                  color: Colors.white,
                ),
              ),
              color: Color(0xFFFFBB0B),
            ),
          ],
        ),
      ),
    );
  }
}

//2nd
class SecondRoute extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xFFFFBB0B),
      body: new Center(
        child: Column(children: <Widget>[
          Container(
            margin: EdgeInsets.fromLTRB(20, 10, 10, 0),
            padding: new EdgeInsets.only(top: 92.0),
            child: Text(
              "Đăng ký",
              style: TextStyle(
                fontSize: 44,
                fontWeight: FontWeight.bold,
                color: Colors.white,
              ),
            ),
          ),
          Container(
            margin: EdgeInsets.only(),
            padding: EdgeInsets.all(25),
            child: TextFormField(
              decoration: new InputDecoration(
                filled: true,
                fillColor: Colors.white,
                labelText: "+84",
                border: OutlineInputBorder(
                  borderRadius: BorderRadius.circular(10),
                ),
              ),
            ),
          ),
          SizedBox(height: 20.0),
          MaterialButton(
            height: 38,
            minWidth: 250,
            shape: RoundedRectangleBorder(borderRadius: new BorderRadius.circular(12)),
            onPressed: () {
              Navigator.push(
                context,
                MaterialPageRoute(builder: (context) => ThirdRoute()),
              );
            },
            child: Text(
              "Tiếp theo",
              style: TextStyle(
                fontSize: 24,
                color: Colors.white,
              ),
            ),
            color: Color(0xFFFFBB0B),
          ),
        ]),
      ),
    );
  }
}

//3rd
class ThirdRoute extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xFFFFBB0B),
      body: new Center(
        child: Column(children: <Widget>[
          Container(
            margin: EdgeInsets.fromLTRB(20, 10, 10, 0),
            padding: new EdgeInsets.only(top: 92.0),
            child: Text(
              "Đăng ký",
              style: TextStyle(
                fontSize: 44,
                fontWeight: FontWeight.bold,
                color: Colors.white,
              ),
            ),
          ),
          Container(
            margin: EdgeInsets.only(),
            padding: EdgeInsets.all(25),
            child: TextFormField(
              decoration: new InputDecoration(
                filled: true,
                fillColor: Colors.white,
                labelText: "Họ và tên",
                border: OutlineInputBorder(
                  borderRadius: BorderRadius.circular(10),
                ),
              ),
            ),
          ),
          Container(
            margin: EdgeInsets.only(),
            padding: EdgeInsets.all(25),
            child: TextFormField(
              obscureText: true,
              decoration: new InputDecoration(
                filled: true,
                fillColor: Colors.white,
                labelText: "Mật khẩu",
                border: OutlineInputBorder(
                  borderRadius: BorderRadius.circular(10),
                ),
              ),
            ),
          ),
          Container(
            margin: EdgeInsets.only(),
            padding: EdgeInsets.all(25),
            child: TextFormField(
              obscureText: true,
              decoration: new InputDecoration(
                filled: true,
                fillColor: Colors.white,
                labelText: "Nhập lại mật khẩu",
                border: OutlineInputBorder(
                  borderRadius: BorderRadius.circular(10),
                ),
              ),
            ),
          ),
          SizedBox(height: 20.0),
          MaterialButton(
            height: 38,
            minWidth: 250,
            shape: RoundedRectangleBorder(borderRadius: new BorderRadius.circular(12)),
            onPressed: () {
              Navigator.push(
                context,
                MaterialPageRoute(builder: (context) => FourthRoute()),
              );
            },
            child: Text(
              "Xác nhận",
              style: TextStyle(
                fontSize: 24,
                color: Colors.white,
              ),
            ),
            color: Color(0xFFFFBB0B),
          ),
        ]),
      ),
    );
  }
}

//4th
class FourthRoute extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xFFFFBB0B),
      body: new Center(
        child: Column(children: <Widget>[
          Container(
            margin: EdgeInsets.fromLTRB(20, 10, 10, 0),
            padding: new EdgeInsets.only(top: 92.0),
            child: Text(
              "Để có trải nghiệm tốt hơn vui lòng bật vị trí thiết bị GPS",
              style: TextStyle(
                fontSize: 35,
                fontWeight: FontWeight.bold,
                color: Colors.white,
              ),
            ),
          ),
          SizedBox(height: 20.0),
          MaterialButton(
            height: 38,
            minWidth: 250,
            shape: RoundedRectangleBorder(borderRadius: new BorderRadius.circular(12)),
            onPressed: () {
              Navigator.push(
                context,
                MaterialPageRoute(builder: (context) => FifthRoute()), //chuyen sang man hinh thu nam
              );
            },
            child: Text(
              "Không, cảm ơn",
              style: TextStyle(
                fontSize: 24,
                color: Colors.white,
              ),
            ),
            color: Color(0xFFFFBB0B),
          ),
          SizedBox(height: 20.0),
          MaterialButton(
            height: 38,
            minWidth: 250,
            shape: RoundedRectangleBorder(borderRadius: new BorderRadius.circular(12)),
            onPressed: () {
              Navigator.push(
                context,
                MaterialPageRoute(builder: (context) => FifthRoute()), //chuyen sang man hinh thu nam
              );
            },
            child: Text(
              "Đồng ý",
              style: TextStyle(
                fontSize: 24,
                color: Colors.white,
              ),
            ),
            color: Color(0xFFFFBB0B),
          ),
        ]),
      ),
    );
  }
}

//5th
class FifthRoute extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xFFFFBB0B),
      body: new Center(
        child: Column(children: <Widget>[
          Container(
            alignment: AlignmentDirectional.center,
            margin: EdgeInsets.fromLTRB(20, 10, 10, 0),
            padding: new EdgeInsets.only(top: 92.0),
            child: Text(
              "Chào mừng bạn đến với app!",
              style: TextStyle(
                fontSize: 35,
                fontWeight: FontWeight.bold,
                color: Colors.white,
              ),
            ),
          ),
        ]),
      ),
    );
  }
}
